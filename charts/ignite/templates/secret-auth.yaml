{{- if and .Values.authentication.enabled .Values.authentication.igniteUserNewPassword }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ignite.fullname" . }}-auth
  labels:
    {{- include "ignite.labels" . | nindent 4 }}
  {{- with (include "ignite.annotations" .) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
type: Opaque
data:
  ignite-user-new-password: {{ .Values.authentication.igniteUserNewPassword | b64enc | quote }}
{{- end }}
